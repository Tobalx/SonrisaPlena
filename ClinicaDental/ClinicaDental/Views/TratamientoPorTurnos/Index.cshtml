@model IEnumerable<ClinicaDental.Models.Entities.TratamientoPorTurno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadoPaso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ObservacionesTurno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Turno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EjecucionTratamiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoPaso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ObservacionesTurno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Turno.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EjecucionTratamiento.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
