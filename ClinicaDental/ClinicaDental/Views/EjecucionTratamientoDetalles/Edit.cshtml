@model ClinicaDental.Models.Entities.EjecucionTratamientoDetalle

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>EjecucionTratamientoDetalle</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="EjecucionTratamientoId" class="control-label"></label>
                <select asp-for="EjecucionTratamientoId" class="form-control" asp-items="ViewBag.EjecucionTratamientoId"></select>
                <span asp-validation-for="EjecucionTratamientoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TratamientoId" class="control-label"></label>
                <select asp-for="TratamientoId" class="form-control" asp-items="ViewBag.TratamientoId"></select>
                <span asp-validation-for="TratamientoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
